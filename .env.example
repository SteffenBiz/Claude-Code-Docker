# MariaDB Konfiguration
MYSQL_ROOT_PASSWORD=change_me_root_password
MYSQL_PASSWORD=change_me_claude_password

# SMTP E-Mail Konfiguration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-smtp-password
SMTP_FROM=Claude Docker <claude@example.com>
SMTP_SECURE=true

# Hinweis: API Keys werden aus den Host-Konfigurationen geladen:
# - Claude API Key: ~/.claude/.credentials.json
# - GitHub Token: ~/.config/gh/hosts.yml